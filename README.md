# Femap Field Coordinate Tool

Simple web app for measuring field coordinates from an image with snap-to-grid and point marking.

Live page: https://aztechell.github.io/femap/

## Run locally

Open `index.html` directly, or run:

```powershell
python -m http.server 8080
```

Then open `http://localhost:8080`.
